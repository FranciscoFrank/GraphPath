# 🔧 Технічна документація GraphPath

## 📐 Архітектура системи

### Загальний огляд

GraphPath побудовано на модульній архітектурі з чіткою сепарацією відповідальностей:

```
┌─────────────────────────────────────────────────────┐
│                    main.c                           │
│  (User Interface & Algorithm Selection)             │
└────────────────┬────────────────────────────────────┘
                 │
         ┌───────┴───────┐
         │               │
    ┌────▼────┐      ┌───▼──────┐
    │ graph.c │      │  Path    │
    └─────────┘      │  Algos   │
                     └──────────┘
                     /    |   \
                    /     |    \
               ┌───▼─┐ ┌──▼──┐ ┌─▼──────┐
               │bfs.c│ │dfs.c│ │dijkstra│
               └─────┘ └─────┘ └────────┘
```

---

## 🗂️ Структури даних

### 1. Graph (Граф)

**Файл:** `include/graph.h:14-20`

```c
typedef struct {
    int num_vertices;      // Кількість вершин
    int num_edges;         // Кількість ребер
    bool is_weighted;      // Чи зважений граф
    bool is_directed;      // Чи орієнтований граф
    Edge** adj_list;       // Масив списків суміжності
} Graph;
```

**Обгрунтування вибору:** Список суміжності (adjacency list)
- ✅ **Ефективність пам'яті:** O(V + E) - оптимально для розріджених графів
- ✅ **Швидкий обхід сусідів:** O(degree(v)) для вершини v
- ✅ **Динамічність:** Легко додавати ребра
- ❌ **Перевірка існування ребра:** O(degree(v)) - повільніше ніж матриця суміжності O(1)

**Альтернативи:**
- **Матриця суміжності:** O(V²) пам'яті - надто багато для великих розріджених графів
- **Список ребер:** O(E) - складніше обходити сусідів вершини

### 2. Edge (Ребро)

**Файл:** `include/graph.h:7-11`

```c
typedef struct Edge {
    int dest;              // Вершина призначення
    double weight;         // Вага ребра
    struct Edge* next;     // Наступне ребро (linked list)
} Edge;
```

**Реалізація:** Однозв'язний список (singly linked list)
- Кожна вершина має свій список ребер
- Додавання нового ребра: O(1) - в початок списку
- Видалення графа: O(E) - проходимо всі списки

### 3. PathResult (Результат пошуку)

**Файл:** `include/graph.h:23-30`

```c
typedef struct {
    int* path;             // Масив вершин шляху
    int path_length;       // Довжина шляху (кількість вершин)
    double total_weight;   // Сумарна вага шляху
    double time_ms;        // Час виконання (мілісекунди)
    const char* algorithm; // Назва алгоритму
    bool found;            // Чи знайдено шлях
} PathResult;
```

**Призначення:** Уніфікований інтерфейс для всіх алгоритмів
- Дозволяє легко порівнювати різні алгоритми
- Містить всю необхідну інформацію для статистики
- Полегшує додавання нових алгоритмів

---

## 🧮 Алгоритми пошуку шляху

### 1. BFS (Breadth-First Search)

**Файл:** `src/bfs.c`

**Коли використовується:**
- Незважені графи
- Гарантовано знаходить найкоротший шлях (за кількістю ребер)

**Реалізація:**
```
1. Створити чергу (queue) - FIFO структура
2. Додати стартову вершину в чергу
3. Поки черга не пуста:
   - Взяти вершину з початку черги
   - Якщо це цільова вершина - шлях знайдено
   - Додати всіх невідвіданих сусідів в кінець черги
```

**Структури даних:**
- `Queue`: Масив розміром `num_vertices`
- `visited[]`: Масив boolean для відстеження відвіданих вершин
- `parent[]`: Масив для відновлення шляху

**Складність:**
- **Часова:** O(V + E)
- **Просторова:** O(V)

### 2. DFS (Depth-First Search)

**Файл:** `src/dfs.c`

**Коли використовується:**
- Незважені графи
- Швидше за BFS в деяких випадках
- НЕ гарантує найкоротший шлях

**Реалізація:**
```
1. Створити стек (stack) - LIFO структура
2. Додати стартову вершину в стек
3. Поки стек не пустий:
   - Взяти вершину з вершини стека
   - Якщо це цільова вершина - шлях знайдено
   - Додати всіх невідвіданих сусідів на вершину стека
```

**Структури даних:**
- `Stack`: Масив розміром `num_vertices`
- `visited[]`: Масив boolean
- `parent[]`: Масив для відновлення шляху

**Складність:**
- **Часова:** O(V + E)
- **Просторова:** O(V)

**Відмінність від BFS:**
- DFS досліджує "вглиб" - йде до кінця одного шляху перед тим як повернутися
- BFS досліджує "вшир" - перевіряє всі сусіди на однаковій відстані

### 3. Dijkstra

**Файл:** `src/dijkstra.c`

**Коли використовується:**
- Зважені графи з невід'ємними вагами
- Гарантовано знаходить найкоротший шлях (за вагою)

**Реалізація:**
```
1. Створити пріоритетну чергу (min-heap)
2. Ініціалізувати відстані: dist[start] = 0, інші = ∞
3. Додати стартову вершину в чергу
4. Поки черга не пуста:
   - Взяти вершину з найменшою відстанню
   - Якщо це цільова вершина - шлях знайдено
   - Для кожного сусіда:
     - Якщо dist[v] + weight(v,u) < dist[u]:
       - Оновити dist[u] (relaxation)
       - Додати u в чергу
```

**Структури даних:**
- `PriorityQueue`: Мін-купа (min-heap) для вибору вершини з найменшою відстанню
- `dist[]`: Масив відстаней від стартової вершини
- `visited[]`: Масив boolean
- `parent[]`: Масив для відновлення шляху

**Складність:**
- **Часова:** O((V + E) log V) з бінарною купою
- **Просторова:** O(V)

**Оптимізація:**
- Early exit: зупиняємося коли знайдено цільову вершину
- Не додаємо вже відвідані вершини в чергу

---

## 🔄 Пріоритетна черга (Priority Queue)

**Реалізація:** Бінарна мін-купа (Binary Min-Heap)

**Файл:** `src/dijkstra.c:9-42`

```c
typedef struct {
    int vertex;
    double distance;
} PQNode;

typedef struct {
    PQNode* nodes;
    int size;
    int capacity;
} PriorityQueue;
```

**Операції:**
- `pq_create()`: O(1)
- `pq_insert()`: O(log V) - heapify вгору
- `pq_extract_min()`: O(log V) - heapify вниз
- `pq_is_empty()`: O(1)

**Heap property:**
```
Parent: nodes[i]
Left child: nodes[2*i + 1]
Right child: nodes[2*i + 2]

Invariant: parent.distance ≤ children.distance
```

---

## 🎯 Вибір алгоритму (Algorithm Selection)

**Файл:** `src/main.c:93-107`

**Логіка:**
```c
if (graph->is_weighted) {
    // Для зважених графів
    algorithms[] = { dijkstra_find_path };
} else {
    // Для незважених графів
    algorithms[] = { bfs_find_path, dfs_find_path };
}
```

**Обгрунтування:**
- **Зважені графи:** Тільки Dijkstra, бо BFS/DFS не враховують ваги
- **Незважені графи:** BFS і DFS для порівняння швидкості

**Майбутні розширення:**
```c
if (has_negative_weights) {
    algorithms[] = { bellman_ford };
} else if (has_heuristic) {
    algorithms[] = { astar };
} else if (all_pairs) {
    algorithms[] = { floyd_warshall };
}
```

---

## ⚙️ Вимірювання часу виконання

**Використання:** `clock()` з `time.h`

```c
clock_t start_time = clock();
// ... алгоритм ...
clock_t end_time = clock();

result->time_ms = ((double)(end_time - start_time) / CLOCKS_PER_SEC) * 1000.0;
```

**Точність:**
- На Unix/Linux: зазвичай мікросекунди
- Достатньо для порівняння швидкості алгоритмів
- Для більш точних вимірювань можна використати `clock_gettime()`

---

## 🧪 Тестування

**Файл:** `tests/test_graph.c`

**Тести:**
1. `test_graph_creation()` - перевірка створення графа
2. `test_edge_addition()` - додавання ребер та валідація
3. `test_bfs_simple_path()` - BFS на простому шляху
4. `test_dfs_simple_path()` - DFS на простому шляху
5. `test_dijkstra_weighted()` - Dijkstra на зваженому графі
6. `test_no_path()` - граф без шляху між вершинами
7. `test_self_loop()` - петля (self-loop)

**Підхід:**
- Використання `assert()` для валідації
- Тести незалежні один від одного
- Кожен тест очищує за собою пам'ять

---

## 🔍 Обробка помилок

**Стратегія:**
1. **Валідація вхідних даних:**
   ```c
   if (num_vertices <= 0) return NULL;
   if (!graph_is_valid_vertex(graph, vertex)) return false;
   ```

2. **Перевірка виділення пам'яті:**
   ```c
   Graph* graph = malloc(sizeof(Graph));
   if (!graph) {
       fprintf(stderr, "Error: Memory allocation failed\n");
       return NULL;
   }
   ```

3. **Graceful degradation:**
   - Якщо шлях не знайдено: `result->found = false`, а не NULL
   - Повертаємо структуру з інформацією про невдачу

4. **Очищення ресурсів:**
   - Завжди викликаємо `destroy()` функції
   - У випадку помилки - очищаємо вже виділені ресурси

---

## 🚀 Оптимізації

### Поточні оптимізації:

1. **Early exit в Dijkstra:**
   ```c
   if (current_vertex == end) {
       break; // Знайшли найкоротший шлях
   }
   ```

2. **Уникнення повторних відвідувань:**
   ```c
   if (visited[neighbor]) continue;
   ```

3. **Calloc для ініціалізації:**
   ```c
   // Автоматично ініціалізує всі елементи нулями
   graph->adj_list = calloc(num_vertices, sizeof(Edge*));
   ```

### Можливі майбутні оптимізації:

1. **Fibonacci heap для Dijkstra:** O((V + E) log V) → O(V log V + E)
2. **Bidirectional search:** Пошук з обох кінців одночасно
3. **A-star з евристикою:** Швидше за Dijkstra для великих графів
4. **Паралелізація:** Запуск кількох алгоритмів паралельно

---

## 📊 Порівняння алгоритмів

| Критерій | BFS | DFS | Dijkstra |
|----------|-----|-----|----------|
| **Часова складність** | O(V + E) | O(V + E) | O((V+E) log V) |
| **Просторова складність** | O(V) | O(V) | O(V) |
| **Гарантує найкоротший шлях** | ✅ (незважені) | ❌ | ✅ (зважені) |
| **Працює з вагами** | ❌ | ❌ | ✅ |
| **Працює з від'ємними вагами** | N/A | N/A | ❌ |
| **Оптимальний для** | Незважені, короткі шляхи | Пошук будь-якого шляху | Зважені графи |

---

## 🔮 Майбутні алгоритми

### 1. A* (A-Star)
- **Коли:** Є евристична функція (наприклад, географічні координати)
- **Складність:** O(E) в середньому, O(V² log V) в гіршому
- **Переваги:** Швидше за Dijkstra в практиці

### 2. Bellman-Ford
- **Коли:** Графи з від'ємними вагами (але без від'ємних циклів)
- **Складність:** O(VE)
- **Переваги:** Детектує від'ємні цикли

### 3. Floyd-Warshall
- **Коли:** Потрібні шляхи між всіма парами вершин
- **Складність:** O(V³)
- **Переваги:** Знаходить всі найкоротші шляхи за один прохід

---

## 💾 Управління пам'яттю

**Правила:**

1. **Кожен create має відповідний destroy:**
   ```c
   Graph* graph = graph_create(...);
   // ... використання ...
   graph_destroy(graph);
   ```

2. **Функції повертають NULL при помилці:**
   - Завжди перевіряємо результат перед використанням

3. **Очищення при помилках:**
   ```c
   Graph* graph = malloc(sizeof(Graph));
   if (!graph) return NULL;

   graph->adj_list = calloc(num_vertices, sizeof(Edge*));
   if (!graph->adj_list) {
       free(graph); // Очищаємо вже виділене
       return NULL;
   }
   ```

4. **Відповідальність за звільнення:**
   - `graph_destroy()` - звільняє граф і всі його ребра
   - `path_result_destroy()` - звільняє результат і шлях

---

## 🛠️ Система збірки

**Файл:** `Makefile`

**Структура:**
```makefile
CC = gcc
CFLAGS = -Wall -Wextra -std=c11 -O2
INCLUDES = -Iinclude
```

**Цілі:**
- `make` - збірка проекту
- `make run` - збірка та запуск
- `make test` - збірка та запуск тестів
- `make clean` - видалення build файлів
- `make help` - довідка
